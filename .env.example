# Sender wallet private key (as JSON array)
# To get your private key from Phantom or other wallets:
# 1. Export your private key from your wallet
# 2. If it's a base58 string, convert it to array format
# 3. For testing, you can use solana-keygen: solana-keygen new --outfile wallet.json
#    Then paste the contents here
SENDER_PRIVATE_KEY=[1,2,3,4,5,...]

# Optional: Set default amount per recipient (can be overridden via command line)
AMOUNT_PER_RECIPIENT=0.01
