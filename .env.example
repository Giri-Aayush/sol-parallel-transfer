# ============================================
# WALLET CONFIGURATION (REQUIRED)
# ============================================

# Sender wallet private key (as JSON array)
# Generate a new wallet: npm run generate-wallet
# Or export from existing wallet and convert to array format
SENDER_PRIVATE_KEY=[1,2,3,4,5,...]


# ============================================
# NETWORK CONFIGURATION
# ============================================

# RPC endpoint for Solana network
# Devnet (for testing - free SOL from faucet):
RPC_ENDPOINT=https://api.devnet.solana.com

# Testnet:
# RPC_ENDPOINT=https://api.testnet.solana.com

# Mainnet (uses real SOL):
# RPC_ENDPOINT=https://api.mainnet-beta.solana.com

# Custom RPC (e.g., QuickNode, Alchemy, Helius):
# RPC_ENDPOINT=https://your-custom-rpc-endpoint.com


# ============================================
# DISTRIBUTION SETTINGS
# ============================================

# Amount of SOL to send per recipient
# Can be overridden via command line: npm start 0.1
AMOUNT_PER_RECIPIENT=0.01

# Path to CSV file containing recipient addresses
# Format: CSV file with 'address' header and one address per line
CSV_FILE=./recipients.csv


# ============================================
# PERFORMANCE SETTINGS
# ============================================

# Number of recipients per transaction (1-25)
# Lower = more transactions but safer
# Higher = fewer transactions but may hit limits
# Recommended: 10
BATCH_SIZE=10

# Number of batches to process in parallel (1-10)
# Higher = faster but may hit RPC rate limits
# Recommended: 5
CONCURRENT_BATCHES=5

# Maximum retry attempts for failed batches (0-5)
# Number of times to retry a failed transaction
# Recommended: 3
MAX_RETRIES=3
